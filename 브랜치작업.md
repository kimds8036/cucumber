# 기능 브랜치 작업 가이드

`develop`에서 잠깐 분기 → 작업 → **체리픽 또는 머지**로 합친 뒤 → 브랜치 삭제하는 흐름입니다.

Railway·production 배포 규칙은 **`푸쉬규칙.md`**, Android 빌드는 **`frontend/BUILD_ANDROID.md`** 를 함께 보세요.

---

## 브랜치 역할

| 브랜치 | 용도 |
|--------|------|
| **`develop`** | 일상 개발·통합 (develop Railway + DB) |
| **`feat/…` / `fix/…`** | 수정 단위 작업 (짧게 쓰고 삭제) |
| **`production`** | 스토어·운영 반영 (별도 규칙) |

`feat/front-init` / `feat/back-init` 장기 브랜치는 **삭제됨.**  
필요할 때마다 `develop`에서 `feat/…` 또는 `fix/…` 만 만듭니다.

### 백엔드만 (예: 개인우편 API)

```powershell
git checkout develop
git pull origin develop
git checkout -b feat/personal-mail-send
# back/ 수정 …
git add back/
git commit -m "feat(back): 개인우편 send·반송·status 마이그레이션"
git push -u origin feat/personal-mail-send
# develop 체리픽 후 브랜치 삭제
# Railway develop DB → cd back && npm run migrate
```

---

## 1. 작업 브랜치 만들기

```powershell
cd c:\cucumber
git checkout develop
git pull origin develop

# 예: 프론트 수정
git checkout -b feat/mail-ui

# 예: 백만 수정
git checkout -b fix/comment-report
```

---

## 2. 작업 · 커밋 · push

```powershell
# … 파일 수정 …

git status
git add <파일 또는 경로>
git commit -m "feat(front): 우편 화면 개선"

git push -u origin feat/mail-ui
```

---

## 3-A. develop에 반영 — **체리픽** (권장, 커밋 단위가 깔끔할 때)

작업 브랜치에 커밋이 몇 개고, 그대로 옮기고 싶을 때:

```powershell
git checkout develop
git pull origin develop

# 해시 확인 (작업 브랜치에서)
git log --oneline feat/mail-ui

# 커밋 하나씩 (여러 개면 순서대로 반복)
git cherry-pick <커밋해시>

# 충돌 나면: 파일 수정 후
git add .
git cherry-pick --continue
# 취소: git cherry-pick --abort

git push origin develop
```

**여러 커밋 한 번에:**

```powershell
git cherry-pick <시작해시>^..<끝해시>
```

---

## 3-B. develop에 반영 — **머지** (브랜치 전체를 한꺼번에)

```powershell
git checkout develop
git pull origin develop

git merge feat/mail-ui -m "merge(feat/mail-ui): develop에 반영"

# 충돌 해결 후
git add .
git commit   # merge 커밋이 자동이면 생략

git push origin develop
```

| 방식 | 장점 | 단점 |
|------|------|------|
| **체리픽** | develop 히스토리 단순, production에도 같은 해시로 옮기기 쉬움 | 커밋마다 충돌 가능 |
| **머지** | 한 번에 합침 | merge 커밋 추가 |

---

## 4. 작업 브랜치 삭제

develop push까지 끝난 뒤:

```powershell
# 원격 삭제
git push origin --delete feat/mail-ui

# 로컬 삭제
git branch -d feat/mail-ui

git fetch origin --prune
```

---

## 5. develop에 다 들어갔는지 확인 (삭제 전)

```powershell
git fetch origin
git log --oneline origin/feat/mail-ui --not origin/develop
```

**아무것도 안 나오면** → develop에 전부 반영된 것. 삭제해도 됨.

```powershell
git merge-base --is-ancestor origin/feat/mail-ui origin/develop
echo $LASTEXITCODE   # 0 이면 포함됨
```

---

## 6. production 반영 (스토어·운영)

- **`develop` 전체 머지 → `production` 은 하지 않습니다.** (`푸쉬규칙.md`)
- develop에 넣은 **같은 커밋**을 production에 **체리픽**:

```powershell
git checkout production
git pull origin production

git cherry-pick <develop에_넣은_해시>

# 로컬만 커밋 후, AAB·Play 절차 끝나면 push
# git push origin production
```

---

## 자주 쓰는 명령 모음

```powershell
# 현재 브랜치
git branch --show-current

# develop 최신화
git checkout develop && git pull origin develop

# 작업 브랜치 목록
git branch -a

# develop vs 작업 브랜치 차이 (파일)
git diff develop..feat/mail-ui --stat

# 방금 cherry-pick 한 커밋 취소 (develop에서, push 전)
git reset --hard HEAD~1
```

---

## 실제 예시 (개인 우편 프론트)

```powershell
git checkout develop
git pull origin develop
git cherry-pick 073e2df
git push origin develop
git push origin --delete feat/front-init
git branch -d feat/front-init
```

→ `073e2df`는 develop에 `f1c7542` 로 반영됨. `feat/front-init` 브랜치는 삭제됨.

---

## 주의

- 작업 브랜치는 **항상 `develop`에서 분기** (production에서 분기하지 않음).
- 민감 파일(`.env`, `credentials.json` 등)은 **커밋하지 않음**.
- 충돌 시 **develop 쪽 최신 규칙**(버전 번호, `apiEnv`, 빌드 스크립트)을 우선하고, 작업 브랜치의 **기능 코드만** 살립니다.
