# Cucumber 레포 작업 규칙

팀·에이전트가 동일한 흐름으로 작업·배포할 수 있도록 정리한 문서입니다.

---

## Railway 환경 (DB·URL 분리)

| 환경 | Railway 서비스(백) | API 베이스 URL | DB / Redis |
|------|-------------------|----------------|------------|
| **Develop** | `cucumber-develop` | `https://cucumber-develop.up.railway.app` | **develop 전용** 변수 그룹 |
| **Production** | `cucumber-production` | `https://cucumber-production.up.railway.app` | **production 전용** 변수 그룹 |

- 프론트는 **브랜치 이름이 아니라** `APP_ENV` / EAS 프로필 / `EXPO_PUBLIC_API_URL`로 어느 Railway를 바라볼지 결정합니다.  
  (`frontend/config/apiEnv.js`, `frontend/eas.json`, `frontend/.env.development` · `.env.production`)
- **`develop` 브랜치** + `APP_ENV=development`(또는 `.env.development`) → **develop Railway + develop DB**
- **`production` 브랜치** + `APP_ENV=production`(또는 EAS `production` 프로필) → **production Railway + production DB**
- develop 브랜치를 production Railway에 붙이거나, production 브랜치를 develop Railway에 붙이면 **데이터·계정·신고 내역이 섞입니다.** 절대 하지 않습니다.

### 강제 업데이트 (버전 체크) Railway 변수

백엔드 `GET /api/app/version-check` 가 `MIN_*` 환경 변수를 읽습니다. **develop / production 서비스에 각각 따로** 넣습니다.

| 변수 | Develop Railway (권장) | Production Railway |
|------|----------------------|-------------------|
| `MIN_ANDROID_VERSION` | `1.0.0` (팀 테스트 차단 방지) | 스토어에 올린 **최소 버전** (예: `1.0.2`) — **AAB 반영 후에만** 올림 |
| `ANDROID_STORE_URL` | Play 링크 (동일 가능) | Play 링크 |
| `NODE_ENV` | `production` 권장 (보안 미들웨어) | `production` |

**순서 주의 (production):** Play에 새 AAB를 먼저 올린 뒤 → `MIN_ANDROID_VERSION`을 올리고 → 그다음 `production` 브랜치를 push해 Railway를 재배포합니다. 순서가 바뀌면 스토어에 없는 버전을 요구해 사용자가 막힐 수 있습니다.

---

## Git 브랜치 전략

| 브랜치 | 역할 |
|--------|------|
| **`feat/front-init`** | 프론트엔드 전용 작업·커밋 |
| **`feat/back-init`** | 백엔드 전용 작업·커밋 |
| **`develop`** | 프론트 + 백 통합·연동 테스트 (develop Railway) |
| **`production`** | 스토어·운영 (production Railway) — **최종 확인 후만** |

### 권장 흐름 (DB/URL 꼬이지 않게)

1. **프론트만** → `feat/front-init`에서 작업 후 **push**
2. **백만** → `feat/back-init`에서 작업 후 **push**
3. 연동 확인 → **`develop`에만** 반영 (**체리픽 권장**, 필요 시 작은 머지) 후 **push** → develop Railway 재배포
4. 스토어·운영 반영 → **`production`에 체리픽** (init/develop 전체 머지 금지) → **로컬 커밋까지** 가능

### `develop` ↔ `production` 반영 규칙

- **`git merge develop` → `production` 은 하지 않습니다.** (API 분기·문서·중간 커밋이 한꺼번에 들어가 URL/설정 추적이 어려워짐)
- **같은 기능 커밋 해시**를 init → develop 체리픽 → production 체리픽 순으로 옮깁니다.
- 체리픽 후 확인: `frontend/config/apiEnv.js`의 URL 표, `app.config.js`의 `extra.apiBaseUrl` / `appEnv`, `.env.production`의 `APP_ENV=production` 이 **production 브랜치에서도 production URL**을 가리키는지 빌드 전에 확인합니다.

---

## `production` 브랜치 push 시점 (중요)

| 단계 | 할 일 | `origin/production` push |
|------|--------|-------------------------|
| 1 | `production` 브랜치에서 AAB 빌드 (`APP_ENV=production`, release 서명) | ❌ 아직 하지 않음 |
| 2 | Play Console에 AAB 업로드·(필요 시) 심사/출시 준비 | ❌ |
| 3 | Production Railway에 `MIN_ANDROID_VERSION` 등 env 정리 (스토어 버전과 일치) | ❌ |
| 4 | 로컬 `production` 커밋·체리픽 완료, 최종 확인 | ✅ **이때 push** → production Railway 재배포 |

- **AAB를 올리기 전에 `production`을 push하면** Railway만 먼저 바뀌고, 스토어 최소 버전·강제 업데이트와 엇갈릴 수 있습니다.
- 에이전트·로컬 작업은 **`production` 브랜치에 커밋까지는 가능**하지만, 사용자가 Play 업로드를 마칠 때까지 **`git push origin production` 은 하지 않습니다.**

---

## 앱 버전 (프론트)

| 항목 | 현재 기준 (develop / production 동일 유지) |
|------|---------------------------------------------|
| `version` | `1.0.2` (`app.json` / `app.config.js`) |
| Android `versionCode` | `4` |

버전을 올릴 때는 **init → develop push → AAB·Play → production 체리픽·push** 순서를 지킵니다.

---

## 로컬에서 화면 테스트할 때

### Develop Railway

```powershell
git checkout develop
git pull origin develop
cd frontend
npm install   # 최초 또는 의존성 변경 시
npm start
# 안드로이드: npm run android
```

### Production Railway (실기기, AAB 전 검증)

```powershell
git checkout production
# origin/production 은 사용자가 push할 때까지 pull 생략 가능
cd frontend
Remove-Item Env:EXPO_PUBLIC_API_URL -ErrorAction SilentlyContinue
npx cross-env APP_ENV=production expo prebuild --platform android --clean
npm run android:prod
```

---

## 관련 경로

- 프론트: `frontend/`
- 백엔드: `back/`
- 강제 업데이트: `back/src/routes/app.js`, `frontend/components/common/ForceUpdateGate.jsx`
- 관리자 웹: `{production 백엔드 URL}/admin`

---

이 문서는 레포 운영 규칙의 요약입니다. 세부 EAS·Play 절차는 `frontend` README·`back/docs`를 함께 참고하세요.
