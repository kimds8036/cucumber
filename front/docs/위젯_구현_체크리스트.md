# Youth Paper 급식/시간표 위젯 구현 체크리스트

> 구현 전 확인용. **이 문서 합의 후** 구현을 시작한다.  
> 원본 요구: 급식/시간표 위젯 (iOS WidgetKit + Android Glance)

---

## 0. 합의된 결정 (2026-08-03)

- [x] 주말 Medium 시간표: **"주말이에요" 플레이스홀더** (A안)
- [x] App Group ID: `group.com.ucost.YouthPaper`
- [x] 딥링크: 시간표 미설정 시 → **마이페이지(`mypage`)**
- [x] 급식 BG 시각: 서버 `next` 끼니 경계와 동일 — **KST 10:00 / 14:00 / 20:00**  
  (`currentMealCodeByHour`: `<10` 조식, `<14` 중식, `<20` 석식, `≥20` 오늘 스킵)
- [x] 시간표 stale 안내: 앱 캐시와 동일 — **`generatedAt` 24h 초과** (`TIMETABLE_CACHE_TTL_MS`)
- [x] 시간표 BG 스케줄: **자정 직후 (00:00~01:00 KST)** best-effort (A안)
- [x] 위젯 탭 딥링크:
  - 급식 Small → **우리학교** (`ourschoolscreen`)
  - 시간표 Medium / Large → **마이페이지** (`mypage`)
  - 시간표 미설정 안내 탭 → **마이페이지**
- [x] 이 체크리스트 기준으로 구현 시작 승인

---

## 절대 하지 말 것 (기존 로직 재구현 금지)

- [x] 급식 20시 컷오프 / 끼니 경계 로직을 클라이언트·위젯에 다시 짜지 않음 → `next?count=3` **0번째만** 사용
- [x] 위젯에서 NEIS 직접 호출 / JWT 리프레시 풀스택을 위젯 프로세스에 넣지 않음
- [x] 시간표 SSOT를 NEIS 원본으로 두지 않음 → **`@mypage_timetable_cache_v1:userId` 완성본 미러링**
- [x] 기존 급식/시간표 API·캐시 로직을 “고쳐 쓰는” 리팩터 금지 → **write 훅만 추가**

---

## 1. 공유 저장소 스키마

- [x] `MealWidgetPayload` (`generatedAt`, `first` — API 항목 그대로: `ymd`/`mealType`/`menus`…)
- [x] `TimetableWidgetPayload` (`generatedAt`, `todayDayLabel` 힌트, `week: TimetableDayLite[5]`, `empty`)
- [x] Medium는 **렌더 시점**에 KST 오늘 요일로 `week`에서 선택 (`todayDayLabel`은 fallback)
- [x] `schoolId` 별도 저장 (`writeSchoolId`) — 급식 BG용 (+ `apiBaseUrl`)
- [x] 민감 토큰과 비민감 payload 저장 경로 분리  
  - 비민감: iOS App Group `UserDefaults` / Android `SharedPreferences`  
  - 민감: iOS Keychain / Android `EncryptedSharedPreferences`

---

## 2. RN ↔ Native 브릿지 (`WidgetBridge`)

- [x] API: `writeMealPayload(json)`, `writeTimetablePayload(json)`, `writeSchoolId(id)`, `reloadWidgets()`
- [x] 토큰 미러: `writeAuthMirror` / `readAuthMirror` / `clearAuthMirror`
- [x] iOS: App Group `UserDefaults` + `WidgetCenter.reloadTimelines`
- [x] Android: `SharedPreferences` + Glance `updateAll`
- [x] Expo local module `youth-paper-widget` + config plugin (`app.plugin.js`)

---

## 3. 앱 쪽 write 훅 (기존 로직 유지, 호출만 추가)

### 급식
- [x] `next` API 성공 직후 → `first = data[0] ?? null` → `writeMealPayload`
- [x] 동일 시점에 `writeSchoolId(schoolId)` (학교 탭 진입 포함)

### 시간표
- [x] flat map → `TimetableDayLite[]` **순수 변환 유틸** (+ `utils/widget/__tests__/payload.test.mjs`)
- [x] `mypage.jsx` hydrate 완료 후 → `writeTimetablePayload`
- [x] `TimetabelChoice` / `EditTimetable` / `TimetableScreen` 저장 완료 후 → `writeTimetablePayload`

### 인증 미러 (시간표 BG용)
- [x] `persist: true` 로그인 시 Refresh + device_id를 보안 저장소에 미러 (`setAuthToken`/`setRefreshToken`)
- [x] 로그아웃 / rememberMe off 시 미러 삭제
- [x] 앱 포그라운드 복귀 시: 위젯 미러 토큰이 더 최신이면 `@auth_token` / `@refresh_token` 덮어쓰기

---

## 4. iOS (WidgetKit)

- [x] App Group `group.com.ucost.YouthPaper` capability (config plugin entitlements)
- [x] Widget Extension 소스 (`modules/youth-paper-widget/targets/YouthPaperWidgets`) — prebuild 시 `ios/YouthPaperWidgets`로 복사  
  ⚠️ **Xcode에서 Widget Extension 타겟이 없으면 1회 수동 추가** (`README_SETUP.md` 참고)
- [x] `MealWidget` Small
- [x] `TimetableWidget` Medium / Large
- [x] UI: 급식 카드 / 오늘·주간 시간표 / 주말·empty·stale 문구
- [x] `BGAppRefreshTaskScheduler` 등록·스케줄 (급식 10/14/20, 시간표 00:15 KST best-effort)

---

## 5. Android (Glance)

- [x] Glance 의존성
- [x] `MealWidgetReceiver` / `TimetableWidgetReceiver`
- [x] Meal 2x2, Timetable 리사이즈(Medium≈4x2 / Large≈4x3)
- [x] WorkManager periodic (급식·시간표) + boot 재스케줄

---

## 6. 백그라운드 갱신

### 6-1. 급식 (하루 3회 @ KST 10/14/20, JWT 불필요)
- [x] `GET /api/schools/:schoolId/meals/next?count=3`
- [x] 저장소의 `schoolId` 사용
- [x] 스케줄: **10:00 / 14:00 / 20:00 KST** (서버 끼니 경계)
- [x] 네트워크 실패 시 재시도 (30분 후 1회, Android)
- [x] 자동 로그인 여부와 무관하게 동작

### 6-2. 시간표 (하루 1회, JWT 필수 — 자정 직후)
- [x] Refresh 미러 없으면 **스킵** (에러 취급 X)
- [x] `POST /api/auth/refresh` → `GET /api/timetable` → 변환 → write → reload
- [x] 새 Access/Refresh를 **보안 미러**에 반영 (+ 포그라운드 시 AsyncStorage 동기화)
- [x] 실행 시각 보장 전제 금지 — 앱 오픈 시 보완
- [x] stale(24h) 안내는 자동로그인 off 사용자용

---

## 7. 엣지 케이스 UI

- [x] 급식 `first: null` → “급식 정보 없음”
- [x] 시간표 캐시 없음 → “시간표를 설정해주세요” + **마이페이지** 딥링크
- [x] 주말 Medium → **“주말이에요”** 플레이스홀더
- [x] `anomalies` 무시, 과목만 표시
- [x] 시간표 stale(24h+) → “최신화하려면 앱을 열어주세요”
- [x] 급식 위젯에는 stale 안내 불필요

---

## 8. 완료 기준 (DoD)

- [ ] 앱에서 급식/시간표 한 번 조회 후 위젯 추가 시 즉시 데이터 표시 — **실기기 재빌드 후 검증** (`YouthPaperWidget` pod 링크 수정 반영)
- [ ] 급식: 앱 미실행이어도 하루 3회 자동 갱신 — **디바이스 검증 필요**
- [ ] 시간표: 자동로그인 on → 하루 1회 BG 갱신 / off → 안내 문구 + 마지막 payload 유지
- [ ] 사용자가 페인팅한 시간표가 위젯에 반영
- [ ] 급식 20시 컷오프 클라이언트 중복 로직 없음
- [ ] BG refresh 후 앱 재실행 시 재인증 실패 없음 (토큰 재동기화)
- [ ] 위젯 탭 → 우리학교(`school`) / 마이페이지(`mypage`) 이동 — **딥링크 폴백 구현됨, 재빌드 후 검증**

> 실기기 empty·딥링크 이슈 추적: `docs/위젯_실기기_이슈_해결_체크리스트.md`

---

## 구현 순서 (합의 후)

1. ~~스키마 + 변환 유틸 (+ 테스트)~~
2. ~~`WidgetBridge` iOS/Android~~
3. ~~RN write 훅 (급식 / 시간표 / schoolId / auth mirror)~~
4. ~~iOS Widget UI~~
5. ~~Android Glance UI~~
6. ~~BG 급식 → BG 시간표~~
7. 엣지 케이스 + DoD 검증 ← **prebuild + 실기기 남음**

---

## 참고 기존 코드 (재구현 금지, 훅만)

| 영역 | 위치 |
|------|------|
| 급식 API | `back/src/routes/schools.js` — `me/meals/next`, `:schoolId/meals/next`, calendar |
| 급식 캐시 | AsyncStorage `@our_school_meal_cache_v1_{schoolId}` (next만, 24h) |
| 시간표 API | `back/src/routes/timetable.js` — `GET /api/timetable` |
| 시간표 SSOT | AsyncStorage `@mypage_timetable_cache_v1:userId` |
| 인증 | `@auth_token`, `@refresh_token`, `@device_id`, `POST /api/auth/refresh` |
| 위젯 모듈 | `front/modules/youth-paper-widget/` |
| JS 유틸 | `front/utils/widget/` |
