# Youth Paper — 릴리스 노트

Play Console **「새 버전의 변경사항」**에 붙여 넣을 때는 각 버전의 **「스토어용 (짧게)」** 블록을 사용하세요.  
Git 추적본: `front/docs/릴리스_노트.md`

---

---

## 1.7.0-dev.1 (develop only) — 2026-09-03

> **스토어 미배포.** develop Railway 반영용 내부 변경.

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 처리방침 | 개인정보처리방침 v1.5.2 — 이니시스 간편인증(01) 실제 수집 항목으로 정정 (DI·성별·내외국인 수집 안 함 명시) |
| 소셜 로그인 | 카카오·애플 가입 UI(`SignKakao`, `SignApple`, `SignProfileUsername`)를 `feat/social-login-signup` 브랜치로 분리 대기 (SDK·비즈 심사 완료 후 흡수 예정) |
| 가입 진입 | `SignupEntry`: 카카오·애플 버튼 제거, 전화번호 단일 경로로 임시 운영 |
| 공지사항 | 관리자 공지사항 CRUD·앱 목록·상세 연동 |
| FCM | 비활성 토큰 90일 정리 cron |
| 관리자 | 전국 학교 분포 지도, 회초리 개선 |
| 법적 문서 | 약관 v1.3.1 / 처리방침 v1.5.2 / 커뮤니티·청소년·오픈소스 v1.1.1 |

---

## 1.7.0 (versionCode 30 / iOS build 30) — 2026-08-31

### 스토어용 (짧게) — 복사용

```
• 문의하기 답변을 앱에서 바로 확인할 수 있습니다
• 로그인 상태가 유지되도록 개선했습니다
• 학생증 검수 결과를 알림으로 받을 수 있습니다
• 급식·시간표 위젯 표시를 다듬었습니다
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 문의 | 인앱 「내 문의」 목록·상세, 관리자 답변 푸시·알림 |
| 로그인 | 항상 자동 로그인 유지, 비밀번호 변경 시 현재 기기 세션 유지 |
| 학생증 | 승인·거절 푸시·알림, 승인 시 앱 진입 |
| 급식 위젯(Android) | 메뉴 최대 9개·글자·줄 간격 조정 |
| 시간표 위젯 | 안내 문구·과목 표시 디자인 정리 |
| 백엔드 | DB 스키마 정규화(관리자 FK 등), 모니터링·크론 개선 |
| 버전 | `app.config.js`·`app.json` 1.7.0 / versionCode·iOS build 30 |

**배포:** AAB·IPA → 스토어 → (필요 시) `MIN_ANDROID_VERSION=1.7.0` / `MIN_IOS_VERSION=1.7.0`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향.

---

## 1.6.3 (versionCode 29) — 2026-08-29

### 스토어용 (짧게) — 복사용

```
• 급식·오늘 시간표 위젯 디자인을 미리보기에 맞게 다듬었습니다
• 긴 과목명은 한 줄로 짧게 보이도록 개선했습니다
• 앱 안 시간표 색을 주간 위젯과 같게 맞췄습니다
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 급식 위젯(Android) | 미리보기 레이아웃 맞춤(아이콘·세로선·배지). 메뉴 8자+`..`, 글자 한 단계 확대 |
| 오늘 위젯 4×2 | 과목 열차 한 줄, 긴 과목 3자+`..`. 시간 표기 `-` |
| 인앱 시간표 | 격자 셀을 4×4 위젯과 동일(과목색 50% / 아이보리 테두리) |
| 버전 | `app.config.js`·`app.json` 1.6.3 / versionCode 29 동기화 |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.6.3` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향. `origin/production` push는 AAB·Play 준비 후.

---

## 1.6.2 (versionCode 28) — 2026-08-29

### 스토어용 (짧게) — 복사용

```
• 타이머 실행 중 화면이 꺼지지 않도록 개선했습니다
• DB에 저장된 시간표가 앱에서 안 보이던 문제를 수정했습니다
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 타이머 | `expo-keep-awake` — 포커스+실행 중만 화면 유지 |
| 시간표 sync | hydrate 시 `ts`를 동기화 시각으로 갱신. 24h TTL로 서버 복구본이 비던 버그 수정 |
| 관리자(백) | 문의 중복 묶음, 등교 현황/오늘 목록, 앱 활동·타이머·미등교 페이지네이션 등(이전 develop 반영분) |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.6.2`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향.

---

## 1.6.1 (versionCode 27) — 2026-08-28

### 스토어용 (짧게) — 복사용

```
• 시간표·위젯 과목 색을 더 다양하게 바꿨습니다
• 위젯에서 과목명이 잘리지 않도록 표시를 다듬었습니다
• 2학기 시간표가 비어 보이던 문제를 완화했습니다
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 시간표/위젯 | 과목 색 13색. 4×2 위젯은 연한 색 제외. 과목명 4글자 표시 |
| NEIS SEM | 학교별 학기·개학 유추 크론. 8월 SEM=2 폴백·반대 학기 재시도 |
| 관리자 | 모니터링 사용자 조회(배지·시간표 DB 저장본·등교·OS)·크론 카드 |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.6.1` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향. `origin/production` push는 AAB·Play 준비 후.

---

## 1.6.0 (versionCode 26) — 2026-08-24

### 스토어용 (짧게) — 복사용

```
• 네트워크가 끊긴 것처럼 잘못 보이던 오프라인 화면 오탐을 줄였습니다
• 시간표 초기화 시 서버에도 반영되도록 수정
• 주말 시간표 위젯이 평일 시간표를 보여주던 문제를 수정
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| OfflineGate | reachable=false 디바운스·포그라운드 복귀 재검사로 오프라인 오탐 완화 |
| 시간표 | 초기화 시 로컬·위젯·서버 override(`PUT {}`) 동기 삭제. hydrate가 `clearedByUser` 존중 |
| 위젯 | 주말(토·일)에 `todayDayLabel` 평일 fallback 금지 → 「주말이에요」 플레이스홀더 |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.6.0` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향. `origin/production` push는 AAB·Play 준비 후.

---

## 1.5.16 (versionCode 25) — 2026-08-20

### 스토어용 (짧게) — 복사용

```
• 홈 화면 위젯(급식·오늘 시간표·주간 시간표)을 추가하고 디자인을 다듬었습니다
• 시간표와 교시 시간을 여러 기기에서 같게 볼 수 있도록 개선
• 급식 위젯이 비어 있는 날을 건너뛰고 다음 급식을 보여주도록 수정
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 위젯 | 급식 2×2, 오늘 시간표 4×2, 주간 시간표 4×4. 교시 경계 자동 갱신 |
| 오늘 위젯 | 왼쪽 위 뱃지 유지, 본문·과목열차를 iOS에 맞춤. 현재 교시 점을 더 진하게 |
| 동기화 | 시간표 override·교시 시간 MySQL 저장 (`GET/PUT /api/timetable`, `/period-times`) |
| DB | `001_init.sql`로 004~010 스쿼시. 기존 DB는 migrate 시 델타 보정 |
| 급식 | 빈 날 스킵 롤링 |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.16` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향. `origin/production` push는 AAB·Play 준비 후.

---

## 1.5.15 (versionCode 24) — 2026-08-03

### 스토어용 (짧게) — 복사용

```
• 회원 탈퇴가 정상적으로 처리되도록 개선
• 탈퇴 후 같은 아이디로 로그인하면 탈퇴 안내가 표시되도록 수정
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 탈퇴 API | `POST /api/auth/withdraw` — soft delete (`is_deleted`/`deleted_at`), PII 삭제, 세션·FCM 무효화 |
| 정책 | 게시글·댓글은 유지(작성자 익명), users 행 hard DELETE 금지 |
| 재로그인 | `ACCOUNT_DELETED` 코드로 「탈퇴한 사용자입니다.」 팝업 |
| 마이그레이션 | `007_users_deleted_at.sql` (`deleted_at` 컬럼) |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.15` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향

---

## 1.5.14 (versionCode 23) — 2026-08-01

### 스토어용 (짧게) — 복사용

```
• 마이페이지에서 학적(학교·학년·반) 정보가 더 바로 반영되도록 개선
• 게시글 답글 작성 중 다른 답글을 눌러도 입력 내용이 유지되도록 수정
• 다크모드 전환을 비활성화해 화면 표시를 안정화
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 마이페이지 | 탭 포커스마다 `/auth/me` 재조회로 관리자·DB 학적 변경 반영 |
| 게시판 | 다른 「답글」 전환 시 `setBottomComment('')` 하지 않음 — 입력 유지 |
| UI | 다크모드 버튼 비활성화 (`userInterfaceStyle: light` 유지) |
| 관리자 | 사용자 학적(학교·학년·반) 변경 API·패널 (앱 버전과 무관, Railway 반영) |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.14` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향

---

## 1.5.13 (versionCode 22) — 2026-07-31

### 스토어용 (짧게) — 복사용

```
• 학생 인증에 나이스+·재학증명서 제출 경로 추가
• 거절 후 다시 제출할 때 안내와 선택 화면을 개선
• 제출 중에는 다른 조작을 막아 실수 방지
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 가입·재제출 | 학생증 외 **나이스+**(앨범 첨부→학생증과 동일 API) / **재학증명서**(가이드→URL·열람번호) 선택 |
| 거절 UX | `StudentVerificationRejected` 정리 · 선택/가이드/나이스+/증명서/문의 셸(`App.js` SafeArea 단일) |
| API | `POST /api/auth/resubmit-certificate` |
| 제출 잠금 | `SubmittingLockModal` — 학생증·나이스+·증명서·가입 마무리 중 뒤로/터치 차단 |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.13` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향

---

## 1.5.12 (versionCode 21) — 2026-07-30

### 스토어용 (짧게) — 복사용

```
• 학교 가입자 수 표시를 50명 미만 / 50명 이상 / 100명 이상 구간으로 정리
• 학생증 촬영 시 학교명·이름이 보이게 찍도록 안내 문구 추가
• 학생증 재제출 화면을 가입 촬영 화면과 같은 형식으로 개선
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 학교 | `formatStudentCount` 최종: ≤50 `50명 미만`, 51~99 `50명 이상`, 이후 `100명 이상` … · 통계 3칸 아이콘/수치 세로 정렬 |
| 학생증 촬영 | 가입·재제출 상단에 학교명·이름 선명 촬영 안내 · 프리뷰 하단 중복 가이드 문구 제거(업로드 상태만 표시) |
| 재제출 UI | `StudentIdResubmit` 레이아웃을 가입 촬영과 맞춤(전체 너비·하단 제출 버튼) · `__DEV__` 미리보기 플래그(`DEV_PREVIEW_STUDENT_ID_RESUBMIT`) |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.12` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향

---

## 1.5.11 (versionCode 20) — 2026-07-29

### 스토어용 (짧게) — 복사용

```
• 학교 가입자 수를 정확한 숫자 대신 구간(50명 미만, 50명 이상, 100명 이상 …)으로 표시
• 게시글 대댓글은 고정되지 않도록 개선 (원댓글만 고정 가능)
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 학교 | `formatStudentCount` — ≤50 → `50명 미만`, 51~99 → `50명 이상`, 이후 100단위 `100명 이상` … · 통계 3칸 세로 정렬(아이콘↑/수치↓)로 간격 정리 |
| 댓글 고정 | 프론트 메뉴에서 대댓글(`parentCommentId`) 고정 숨김 · API도 대댓글 pin 거절 |
| 관리자 | 학생증 거절 후 재승인 · 문의 처리 이력 답변 드롭다운 (앱 버전과 무관, Railway 반영) |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.11` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향

---

## 1.5.10 (versionCode 19) — 2026-07-27

### 스토어용 (짧게) — 복사용

```
• 네트워크가 끊기지 않았는데 오프라인 화면이 뜨던 문제 수정
• 앱을 다시 켰을 때 연결 상태를 더 안정적으로 확인
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| OfflineGate | `isInternetReachable=false` 일시 오탐 debounce · 포그라운드 복귀 grace · fetch 세대 가드 |
| OfflineGate | NetInfo 리스너/수동 새로고침 경로 정리 (`front/docs/오탐 수정.md`) |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.10` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향

---

## 1.5.9 (versionCode 18) — 2026-07-27

### 스토어용 (짧게) — 복사용

```
• 알림 토스트를 누르면 해당 화면으로 더 정확하게 이동하도록 개선
• 친구 요청·게시글 알림을 눌렀을 때 화면이 바로 닫히던 문제 수정
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 토스트 | 게시글 알림: `category=post` / `relatedType=post` 모두 `BoardDetail`로 이동 (알림 목록과 조건 통일) |
| 토스트·푸시 | 우편 반송·개인 우편·FCM `targetScreen` 매핑 보정 (`message_room`→Chat 등) |
| 네비 | 푸시/토스트가 Main 위에 상세를 올린 뒤, 언포커스 Main에서 탭 `navigate`로 상세가 가려지던 문제 수정 (`MainTabNavigator` pending tab) |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.9` / `MIN_IOS_VERSION`  
**주의:** Production `MIN_*`는 스토어 반영 **이후**에만 상향

---

## 1.5.8 (versionCode 17) — 2026-07-19

### 스토어용 (짧게) — 복사용

```
• 회원가입 재학 정보(학교·학년·반) 입력 화면 개선
• 학적 확인 안내 문구를 더 이해하기 쉽게 수정
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 회원가입 | 재학 정보 입력(학교 선택) UI 정리 |
| 회원가입 | 학적 확인·학년 관련 모달 문구 수정 |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.8` / `MIN_IOS_VERSION`

---

## 1.5.7 (versionCode 16) — 2026-07-18

### 스토어용 (짧게) — 복사용

```
• 회원가입 시 학교·학년·반을 확인하고 진행하도록 개선
• 회원가입 전 학생증·재학증명서 준비물 안내 추가
• 타이머 화면 공유 이미지 저장이 실패하던 문제 수정
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 회원가입 | 학교 선택 화면에 생년월일 기준 **학년 잠금** + **반 입력**, 「이 학년이 아니신가요?」 문의 안내 |
| 회원가입 | 학생증 단계 전 학교·학년·반 확인 모달 (배경 탭 닫기 불가) |
| 로그인 | 「회원가입」 전 준비물 팝업 (학생증 / 네이버 재학증명서) |
| 타이머 캡처 | Android 번들 워터마크 URI에 `Image.prefetch` → `unsupported url scheme` 저장 실패 수정 |
| 수정 | http(s)만 prefetch, 로컬 require 에셋은 `<Image onLoad>`로 디코드 대기 |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.7` / `MIN_IOS_VERSION`

---

## 1.5.6 (versionCode 15) — 2026-07-16

### 스토어용 (짧게) — 복사용

```
• 구버전 강제 업데이트 시 스플래시에 멈추던 문제 수정
• 업데이트 안내 화면이 정상적으로 표시되도록 개선
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 강제 업데이트 | `SplashHideWhenReady`를 `ForceUpdateGate` 밖으로 이동 — force 시 hideAsync 미실행 버그 수정 |
| 이중 안전 | `ForceUpdateGate` force/error 시 `SplashScreen.hideAsync` 직접 호출 |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.6` / `MIN_IOS_VERSION`

---

## 1.5.5 (versionCode 14) — 2026-07-16

### 스토어용 (짧게) — 복사용

```
• 회원가입 본인인증(KG 이니시스) 정상 연동 수정
• 스토어 빌드에서 테스트용 본인인증이 노출되지 않도록 개선
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| AAB env | `android:aab:prod`에 `EXPO_PUBLIC_INICIS_ENABLED=true` 누락 수정 |
| 가드 | `gradle-bundle-release.mjs` — INICIS/가입테스트/성인완화 플래그 검사 후 실패 |
| 문서 | `front/docs/빌드_EXPO_PUBLIC_체크리스트.md`, env:print 한글 ON/OFF 안내 |

**배포:** AAB → Play → (필요 시) `MIN_ANDROID_VERSION=1.5.5` / `MIN_IOS_VERSION` → Railway `INICIS_ENABLED=true` 확인

---

## 1.5.4 (versionCode 13) — 2026-07-16

### 스토어용 (짧게) — 복사용

```
• Android 스플래시 로고 고해상도 적용으로 선명도 개선
• 스플래시 표시 방식 정리 (기기별 비율 대응)
• 전반적인 안정성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| Android 스플래시 | `splash-icon-android.png` 880×692, `imageWidth: 220`, contain |
| iOS 스플래시 | 기존 `splash-icon.png` 유지 |
| 설정 | `app.json` / `app.config.js` splash `cover`→`contain` 일치 |
| — | Fade-in 실험 후 네이티브 스플래시만 유지로 원복 |

**배포:** AAB → Play → (필요 시) production DB migrate → `MIN_ANDROID_VERSION=1.5.4` → `git push origin production`

---

## 1.5.3 (versionCode 12) — 2026-07-16

### 스토어용 (짧게) — 복사용

```
• 학생증 승인 대기 화면: Youth Paper 안내 문구로 통일
• 승인 대기 화면 UI 정리 (불필요 요소 제거)
• 전반적인 사용성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 학생증 대기 | `StudentVerificationGate` — 「오이」→ Youth Paper, 이모지·로그아웃 버튼 제거 |
| — | 승인 상태 폴링·문구만 유지 |

**배포:** AAB → Play → (필요 시) production DB migrate → `MIN_ANDROID_VERSION=1.5.3` → `git push origin production`

---

## 1.5.2 (versionCode 11) — 2026-07-15

### 스토어용 (짧게) — 복사용

```
• 앱 시작 스플래시 화면·로고 표시 개선
• 설정: 다크모드는 준비 중으로 안내 (오작동 방지)
• 전반적인 안정성 및 사용성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 스플래시 | `resizeMode: contain`, splash 플러그인 옵션·이미지 갱신 |
| 설정 | 다크모드 토글 비활성화 → 「준비 중인 기능」 토스트 |
| 가입(dev) | `SIGNUP_TEST_MODE` 기본 OFF(정상 플로우). `ADULT_TEST_MODE`로 성인 연령 완화는 `__DEV__`만 |
| env | `.env.example` 안내 보강, device/ios 스크립트에 signup env 반영 |

**배포:** AAB → Play → (필요 시) production DB migrate → `MIN_ANDROID_VERSION=1.5.2` → `git push origin production`

---

## 1.5.1 (versionCode 10) — 2026-07-15

### 스토어용 (짧게) — 복사용

```
• 앱 실행 시 Youth Paper 스플래시를 유지한 뒤 바로 로그인 화면으로 진입
• 게시글 공유: 앱 바로가기 링크 및 Play·App Store 설치 안내
• 게시판: 당겨서 새로고침·탭 복귀 시 최신 글 반영
• 약관·개인정보처리방침 표 표시 및 사용성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 부트 UX | 네이티브 스플래시 유지 중 버전 확인·인증 준비. 「버전 확인 중」·로그인 스켈레톤 UI 비표시 |
| 공유 | `youthpaper://board/:id` + Play / App Store(`id6770454607`) 안내 |
| 게시판 | Pull to Refresh, Focus 1분 쿨다운 1페이지 교체, Tip 문구 갱신 |
| 법적 문서 | 마크다운 표를 실제 표 UI로 렌더 |
| 스토어 | 강제업데이트 iOS 기본 스토어 URL = App Store |

**배포:** AAB → Play → (필요 시) production DB migrate → `MIN_ANDROID_VERSION=1.5.1` → `git push origin production`

---

## 1.5.0 (versionCode 9) — 2026-07-14

### 스토어용 (짧게) — 복사용

```
• 회원가입: KG 이니시스 본인인증·보호자 인증·재학증명서 가입 개선
• 이용약관·개인정보처리방침 등 법적 문서 앱 내 최신 표시
• 개인우편: 자동 반송 안내 및 재발송 경험 개선
• 타이머·시간표·등교 배너 안정성 개선
• 학생증 검수 대기·거절 화면에서 로그아웃·문의 지원
• 보안·오류 처리 강화 및 전반적인 사용성 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 회원가입 | 이니시스 간편인증 `01`, 보호자 경로, 재학증명서 signup·관리자 검수, 성인 테스트 모드 원복, SKIP은 `__DEV__`만 |
| 법적 문서 | `legal_documents` DB + 관리자 편집, 커뮤니티 가이드·청소년보호·오픈소스 통합 |
| 개인우편 | 반송 대기 기본 3시간, cron 30분 주기 |
| 타이머 | 날짜 전환 스켈레톤, 미래일 이동 차단, 캡처 워터마크 |
| 검수 UX | PENDING/REJECTED 로그아웃, 거절 시 문의하기 |
| 보안 | Access JWT 기본 15m + refresh, validation 2b, multer MIME·용량, Login 검수 기본값 PENDING |
| 관리자 | 수동 학생 계정 생성, 약관 저장 audit 수정 |

**배포:** AAB → Play → production DB migrate → `MIN_ANDROID_VERSION=1.5.0` → `git push origin production`

---

## 1.4.0 (versionCode 8) — 2026-05-29

### 스토어용 (짧게) — 복사용

```
• 회원가입: 본인확인·계정·학생증 단계 정리 및 입력 안내 개선
• 학생증 검수: 승인 전 대기 화면·재제출 지원
• 관리자: 학생증 검수·OTP 2FA 개선
• 앱 업데이트 안내 및 보안 강화
• 전반적인 오류 수정 및 성능 개선
```

### 상세 (팀·내부)

| 구분 | 내용 |
|------|------|
| 회원가입 | 4단계(약관→본인확인→계정→학생증), 학교 검색 UI, 아이디·비밀번호 규칙 실시간 안내 |
| 학생증 검수 | PENDING 게이트, 재제출 화면, 관리자 학생증 탭 SQL 수정 |
| 강제 업데이트 | App-Version 헤더·426 미들웨어(방법 A) + ForceUpdateGate(방법 B) |
| 관리자 | OTP 2FA, 환경 드롭다운, admin HTML Railway 배포 경로 정리 |
| 백엔드 | `requireMinAppVersion`, 마이그레이션 041 (`signup_student_id_submissions`) |

**배포:** AAB 업로드 → 출시 → Railway `npm run migrate` (041) → `MIN_ANDROID_VERSION=1.4.0` → `production` push. 앱·백엔드 동시 배포 권장.

---

## 1.3.0 (versionCode 7) — 2026-05-29

### 스토어용 (짧게) — 복사용

```
• 회원가입: 전화번호 인증 및 학생증 인식으로 간편 가입
• 앱 사용 가이드 추가
• 시간표 미리보기·과목 매칭 개선
• 개인우편·채팅·게시판 UI 개선
• 사용자 차단·신고 처리 개선
• 전반적인 오류 수정 및 성능 개선
```

(이전 버전 상세는 git 히스토리·아카이브 참고)
