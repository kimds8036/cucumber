# pre-squash archive

2026-07 스쿼시 이전 `001_initial_schema.sql` ~ `059_*.sql` (65개).

**실행되지 않습니다.** `migrate.js`는 `migrations/` 루트의 `.sql`만 읽습니다.

참고·감사용으로만 보관합니다. 현재 스키마는 `../001_init.sql`을 기준으로 하세요.
